import { JuegoAdivinaProvincia } from './juego-adivina-provincia';

describe('JuegoAdivinaProvincia', () => {
  it('should create an instance', () => {
    expect(new JuegoAdivinaProvincia()).toBeTruthy();
  });
});
